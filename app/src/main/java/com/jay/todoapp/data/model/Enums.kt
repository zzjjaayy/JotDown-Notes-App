package com.jay.todoapp.data.model

enum class Priority {
    HIGH, MEDIUM, LOW
}

enum class SortOrder {
    LATEST_FIRST, OLDEST_FIRST, HIGH_PRIORITY, LOW_PRIORITY
}

enum class ListSource {
    MAIN, ARCHIVE
}